name: Deploy site to GitHub Pages

on:
  push:
    branches: ["main", "claude/travel-countdown-app-cfh2cq"]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Turn on Pages for this repository
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - name: Assemble the static site
        run: |
          mkdir -p _site
          cp index.html styles.css app.js data.js skylines.js _site/
          touch _site/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - id: deployment
        uses: actions/deploy-pages@v4
